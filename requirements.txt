PySide6>=6.6
keyboard>=0.13.5
pyperclip>=1.8.2
pywin32>=306
uiautomation>=2.0.20
requests>=2.31
pyinstaller>=6.3

# Optional, only needed if you set active_backend to "huggingface" in
# config.json -- NOT installed by default since torch is large and the
# correct build depends on your GPU/CUDA setup. See README.
# transformers
# torch
